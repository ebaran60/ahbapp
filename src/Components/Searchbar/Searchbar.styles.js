import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        margin:5,
        padding:1,
        backgroundColor: 'white',
        borderRadius: 15,
        flexDirection:'row',
    },
    input:{
        flex:1,
    },
});