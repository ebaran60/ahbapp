import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        alignItems:'center'
    },
    image: {
        width:353,
        height:175,
        resizeMode: 'contain',
        backgroundColor: 'blue',
        borderRadius:13,
    },
    views: {
        flexDirection:'row',
    }
});