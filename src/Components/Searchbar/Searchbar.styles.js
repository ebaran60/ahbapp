import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        margin:5,
        padding:1,
        backgroundColor: '#F1F1F1',
        borderRadius: 15,
        flexDirection:'row',
        padding:5
    },
    input:{
        flex:1,
    },
});